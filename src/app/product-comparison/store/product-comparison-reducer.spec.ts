import { ProductComparisonReducer } from './product-comparison-reducer';

describe('ProductComparisonReducer', () => {
  it('should create an instance', () => {
    expect(new ProductComparisonReducer()).toBeTruthy();
  });
});
