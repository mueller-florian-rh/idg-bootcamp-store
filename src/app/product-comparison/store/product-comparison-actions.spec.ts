import { ProductComparisonActions } from './product-comparison-actions';

describe('ProductComparisonActions', () => {
  it('should create an instance', () => {
    expect(new ProductComparisonActions()).toBeTruthy();
  });
});
